/**
 * Number of items per row depending for each breakpoint.
 *
 * @type {{xl: number, md: number, sm: number, xs: number, lg: number}}
 */

export const composition = {
  xs: 2,
  sm: 3,
  md: 3,
  lg: 3,
  xl: 4,
};
