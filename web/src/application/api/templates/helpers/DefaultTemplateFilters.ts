import { TemplateFilters } from "../../../../model/Template";

/**
 * Default template query filters.
 * @type {TemplateFilters}
 */
const DefaultTemplateFilters: TemplateFilters = {
  name: "",
};

export default DefaultTemplateFilters;
