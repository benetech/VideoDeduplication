import queriesInitialState from "../queries/initialState";

/**
 * Initial State for template-matches (objects) management.
 */
const initialState = {
  /**
   * Cached template-match queries.
   */
  queries: queriesInitialState,
};

export default initialState;
